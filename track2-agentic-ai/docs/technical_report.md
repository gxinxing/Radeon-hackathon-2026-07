# Technical Report: Crypto Trading Agent on AMD Radeon GPU

**AMD AI DevMaster Hackathon 2026 — Track 2: Agentic AI**

## 1. Target Application Definition

This project builds an **autonomous crypto trading agent** that reasons like an
experienced human trader. Users describe trading strategies in natural language;
the agent generates a formal strategy DSL, backtests it on historical market data,
evaluates risk, and optionally executes paper trades on Binance Testnet.

The system addresses a real-world problem: retail traders often have trading ideas
but lack the technical skills to formalize them into testable strategies. By
bridging natural language and executable trading logic, this agent democratizes
quantitative trading.

## 2. Overall System Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                         Dify Chat UI                              │
│  User natural language input → Agent response with analysis       │
└──────────────────────────┬────────────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────────────┐
│                   Dify Workflow Engine                              │
│  LLM(NL→DSL) → Code(Validate) → HTTP(Backtest) → LLM(Report)     │
│       │                                          │                │
│  Knowledge Base                          Tools (API)               │
│  (trading rules, RAG)                (market data, indicators)     │
└──────────────────────────┬────────────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────────────┐
│                AMD ROCm GPU (51 GB VRAM)                           │
│                                                                    │
│  ┌────────────────────────┐    ┌────────────────────────────────┐ │
│  │ vLLM (ROCm)            │    │ QLoRA Fine-tuning              │ │
│  │ Qwen2.5-7B (merged)    │    │ FNSPID + FinGPT + NL→DSL      │ │
│  │ FP16, ~56 t/s          │    │ PEFT merge → vLLM serve         │ │
│  └────────────────────────┘    └────────────────────────────────┘ │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │ Freqtrade Backtest Engine + CCXT + TA-Lib                    │ │
│  └──────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────┘
```

### Key Design Decisions

| Component | Choice | Rationale |
|-----------|--------|-----------|
| Base model | Qwen2.5-7B-Instruct | Native Chinese support, ROCm-compatible, strong code generation |
| Fine-tuning | QLoRA 4-bit (NF4) | Fits 51GB VRAM, preserves base model knowledge |
| Inference | vLLM with merged LoRA | V1 engine for performance, avoids V0 LoRA fallback |
| Agent framework | Dify | Visual workflow, OpenAI-compatible API, custom tools, chat UI |
| Strategy DSL | YAML + JSON Schema | LLM-friendly, human-readable, validatable |
| Backtest | Custom engine + CCXT/TA-Lib | Crypto-native, no Freqtrade installation dependency |
| Market | Crypto (Binance) | 24/7 trading, open API, Testnet for paper trading |

## 3. How AMD Radeon GPUs Are Utilized

### LLM Inference (vLLM on ROCm)

- **Model**: Qwen2.5-7B-Instruct, fine-tuned with QLoRA
- **Engine**: vLLM with ROCm support (official since v0.14.0)
- **Configuration**: FP16, `gpu_memory_utilization=0.60`, `enforce_eager` mode
- **Environment**: `ROCBLAS_USE_HIPBLASLT=1` for optimized matrix operations
- **Performance**: ~56 tokens/s on AMD Radeon GPU (51GB VRAM)
- **VRAM Usage**: ~14GB for model + KV cache, leaving 37GB for Track 3

### Model Fine-tuning (QLoRA on ROCm)

- **Method**: 4-bit NF4 quantization + LoRA (r=64, alpha=128)
- **Framework**: PEFT + TRL (SFTTrainer)
- **Training Data**: 80K+ samples (FNSPID + FinGPT + NL→DSL pairs)
- **Training Time**: ~2-4 hours for 3 epochs on AMD Radeon GPU
- **Peak VRAM**: ~20GB during training
- **Post-training**: LoRA weights merged into base model for V1 engine inference

### FreqAI (Optional ML Signal Enhancement)

- Freqtrade's FreqAI module uses PyTorch for ML model training
- Directly compatible with ROCm PyTorch
- Can train XGBoost/LightGBM prediction models on AMD GPU

## 4. Innovation and Key Technical Contributions

### 4.1 Trading Strategy DSL

A novel YAML-based domain-specific language for expressing crypto trading
strategies. The DSL serves as an intermediate representation between LLM
output and executable code:

- **LLM-friendly**: YAML structure is reliably generated by fine-tuned LLMs
- **Validatable**: JSON Schema ensures structural correctness before execution
- **Transpilent**: Automatically converts to Freqtrade IStrategy Python classes
- **Human-readable**: Traders can review and modify the DSL before execution

### 4.2 Fine-tuned Trading LLM

Qwen2.5-7B fine-tuned on a curated dataset of:
- Financial news analysis (FNSPID) — market sentiment understanding
- Financial QA (FinGPT) — trading knowledge and reasoning
- NL→DSL instruction pairs (2K+ synthetic examples) — strategy generation

This creates a model that understands both trading concepts and the DSL format,
bridging the gap between human trading intuition and machine-executable strategies.

### 4.3 Full-Chain Autonomous Pipeline

The system implements a complete pipeline from idea to execution:
1. **NL Input** — User describes a strategy in natural language
2. **DSL Generation** — LLM generates structured YAML strategy spec
3. **Schema Validation** — JSON Schema validates structure and semantics
4. **Backtest** — Strategy is backtested on 180 days of historical data
5. **Risk Assessment** — LLM evaluates drawdown, Sharpe, win rate
6. **Report** — Natural language analysis with recommendations
7. **Paper Trade** — Optional deployment to Binance Testnet

## 5. Datasets Used

| Dataset | Source | Size | Purpose |
|---------|--------|------|---------|
| FNSPID | HuggingFace (Zihan1004/FNSPID) | 50K samples | Financial news → sentiment analysis |
| FinGPT | HuggingFace (AI4Finance) | 5K samples | Financial QA, reasoning |
| NL→DSL pairs | Synthetic (template-based) | 2K samples | NL → strategy DSL generation |
| Market data | CCXT (Binance API) | 180 days OHLCV | Backtesting |
| Trading rules | Manual curation | 500 rules | System prompt / RAG knowledge |

## 6. Final Deliverables

| Deliverable | Location | Status |
|-------------|----------|--------|
| Fine-tuned model | `models/qwen-trader-merged/` | Train on GPU instance |
| DSL schema | `src/dsl/schema.json` | ✅ Complete |
| DSL validator | `src/dsl/validator.py` | ✅ Complete |
| DSL transpiler | `src/dsl/transpiler.py` | ✅ Complete |
| Backtest microservice | `src/backtest/server.py` | ✅ Complete |
| Backtest runner | `src/backtest/runner.py` | ✅ Complete |
| Market data tools | `src/tools/market_data.py` | ✅ Complete |
| Indicator calculator | `src/tools/indicators.py` | ✅ Complete |
| Paper trading | `src/tools/paper_trade.py` | ✅ Complete |
| LLM prompts | `src/llm/prompts.py` | ✅ Complete |
| QLoRA training script | `training/scripts/train_qlora.py` | ✅ Complete |
| LoRA merge script | `training/scripts/merge_lora.py` | ✅ Complete |
| vLLM serving script | `training/scripts/serve_vllm.sh` | ✅ Complete |
| Dify workflow guide | `dify/workflows/SETUP_GUIDE.md` | ✅ Complete |
| OpenAPI spec | `dify/tools/trading_api_openapi.yml` | ✅ Complete |
| Docker compose | `docker/docker-compose.yml` | ✅ Complete |
| Setup script | `scripts/setup.sh` | ✅ Complete |
| E2E verification | `scripts/verify_e2e.sh` | ✅ Complete |
| Unit tests | `tests/` | ✅ 22 tests passing |
| Technical report | `docs/technical_report.md` | ✅ This document |
| Demo video | `demos/` | To be recorded |

## 7. Team

- Team Name: [TODO: fill before submission]
- Members: [TODO: fill before submission]
