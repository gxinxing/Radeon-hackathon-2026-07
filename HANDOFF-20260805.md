# 交接文档 — 2026-08-05 17:42 (Asia/Shanghai)

> 电脑电量告急，紧急交接。当前两件最重要的事：**GitHub 足球提交仓库整理（进行中）** + **备选 Franka（已完成，待补材料）**。

## 1. 总览
- 比赛：AMD AI DevMaster 2026 Track3，截止 **2026-08-06 23:59 北京时间（明天）**
- 双线：足球 3v3 主线（codely 跟进，勿干预）+ Franka 备选（已完成、远端实测通过、未提交 PR）
- 今日任务：整理 GitHub 主提交仓库 `gxinxing/Radeon-hackathon-2026-07-track3`，整理完后克隆到另一 GitHub 账号提交

## 2. GitHub 仓库整理进度（进行中：已同步未提交）
- 本地规范源（最新，唯一可信源）：`/Users/simon/Documents/01_AI and Code Development/Radeon-hackathon-2026-07/`，head `97ca0ac`，足球内容在子目录 `track3-soccer/`（147MB，runs/archive/track3-data 占大头）
- GitHub 仓库默认分支 `track3-final` 旧状态 @`b8fc1fe`（7/31），混入 track2 92 文件 + 脏文件 `"ema_slow, short: null}\n  exit: {long: ema_fast"` + sync.log/train_v8.log
- **已完成**：克隆到 `/tmp/track3-gh-work` → 清空旧内容 → 按白名单从规范源同步 218 个文件（24MB）→ 已 `git add` 暂存。**未 commit 未 push**
- 白名单 = 规范源已跟踪文件，排除 `archive/ fallback/ track3-data/ orchestration/ ops/`、`*.log`、`sync*`、`MEMORY.md`、坏符号链接 `urdf/t1/meshes`（指向远端 /workspace，本地不存在）

### 剩余动作（约 5 分钟，可随时续做）
```bash
cd /tmp/track3-gh-work   # 若 /tmp 被清则先重新 clone（见下方"重做"）
# 1) force-add 11 个被 .gitignore 忽略但规范源已跟踪的演示视频/onnx
git add -f acceptance/demo/shared_physics_smoke_failed.mp4 \
  amd-physical-ai-soccer/scripts/patch_worker_kick.sh \
  demos/eval_soccer_20260805.mp4 demos/match_1v1_20260805.mp4 \
  demos/match_3v3_20260805.mp4 demos/match_3v3_final_20260805.mp4 \
  demos/match_3v3_fixed_20260805.mp4 models/chase_v9_policy.onnx \
  scripts/render_shared_physics_video.py \
  src/booster_agent/models/chase_v3_policy.onnx \
  src/booster_agent/models/chase_v6_policy.onnx
# 2) 提交（仓库本地 user.name 已设为 gxinxing）
git commit -m "chore: clean soccer-only Track 3 submission content (sync local canonical 97ca0ac)"
# 3) 推送
git push origin track3-final
# 4) 验证：全新 clone，确认无 track2、无垃圾文件、README 正常
```

### 若 /tmp 丢失，重做命令（规范源在 Documents 永不丢）
```bash
git clone git@github.com:gxinxing/Radeon-hackathon-2026-07-track3.git /tmp/track3-gh-work
cd "/Users/simon/Documents/01_AI and Code Development/Radeon-hackathon-2026-07"
git ls-files track3-soccer/ | sed 's#^track3-soccer/##' | grep -v -e '^archive/' -e '^fallback/' -e '^track3-data/' -e '^orchestration/' -e '^ops/' -e '\.log$' -e '^sync\.log$' -e '^sync_from_remote\.sh$' -e '^MEMORY\.md$' -e '\.DS_Store$' -e '^urdf/t1/meshes$' > /tmp/clean_filelist
rsync -a --files-from=/tmp/clean_filelist "…/track3-soccer/" /tmp/track3-gh-work/
cp "…/track3-soccer/.gitignore" /tmp/track3-gh-work/.gitignore
```

## 3. GitHub 仓库分支现状
- `main` @`55ca69a`：纯足球但旧（8/3），含 archive 视频、`问题全景梳理.docx`
- `track3-final` @`b8fc1fe`：默认分支，7/31 旧 + track2 混合 ← **整理目标，全部动作只动它**
- `track3-final-clean` @`f839fa0`、`track3-push` @`db1369e`：均含 track2 115 文件，不动

## 4. 关键成果数据（提交材料/视频脚本用）
- 1v1：200 步、0 falls、球位移 12.046m、rl_reward 77.86
- 3v3 booster：30 步、6 机器人、0 fallen，但 kicks=0 / 球位移=0（未进球，主线卡点，codely 在调）
- 远端最新（09:16）`match_booster_result.json` 反而 fallen=6（退步，codely 正在调，**勿干预**）
- 演示视频在 `track3-soccer/demos/`（6 个 20260805 视频）+ `acceptance/demo/track3_3v3_20260804.mp4`

## 5. 连接与凭据
- 远端 AMD GPU：`https://radeon-global.anruicloud.com/instances/<REDACTED>/lab`；`/workspace` 活跃区 `scripts/ demo_artifacts/ strategy/ rh26-test/`，历史 `/workspace/archive-20260805/`
- 远端执行通道：`/tmp/jupyter_exec.py`（stdin 喂代码）+ `/tmp/jupyter_upload.py`（上传）——也在 /tmp，若丢需重建
- gh 已认证 `gxinxing`（keyring，repo 权限）；git email `gxinxing2014@gmail.com`，user.name 需在仓库本地设
- 审计限制：`rm -rf` 会被拒，用 `mv` 或唯一新目录名

## 6. 备选 Franka（track3-2，独立仓库）
- 已完成，远端实测通过，**未提交 PR**（用户明确"提交 pr 先不要"，作为备选）
- 待补：`TECHNICAL_REPORT.md` 第 8 节团队成员仍是占位符；比赛视频需 3-5 分钟（现太短，说服力不够）

## 7. 用户偏好（重要）
- 不要只写指导文档不执行——动手做，文档只是交接
- 不搞定时盯进度；文件管理要收敛（GitHub/远端/本地三处太多，用户判断不了新旧）
- 足球 3v3 主线归 codely，我只辅助盯进度，不插手其调试
