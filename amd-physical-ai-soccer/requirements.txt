# NOTE: install torch separately, matched to the instance ROCm version, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/rocm6.2
# Do NOT pin torch here or you may pull the CUDA/CPU build.

genesis-world>=0.2.0
skrl>=1.3.0
numpy>=1.26
pyyaml>=6.0
tensorboard>=2.16
imageio[ffmpeg]>=2.34     # export demo mp4
tqdm>=4.66
